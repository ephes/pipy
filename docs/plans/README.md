# Supporting implementation records

The sole active planning basis is [docs/backlog.md](../backlog.md).
Files in this directory retain feature-specific implementation rationale linked
from current contracts. They do not own priorities or the next task. Completed
large program plans were removed; use Git history to retrieve them if needed.
Do not create a competing roadmap here. A future selected task may have a small
reviewed spec when its contract warrants one; link it from its backlog task.

Removed completed programs: `docs/architecture-migration.md`,
`docs/specs/2026-07-24-architecture-quality-improvement-plan.md`,
`docs/plans/2026-07-30-comparative-review-remediation-plan.md`, and
`docs/plans/2026-08-03-god-file-decomposition-plan.md`. The dated
`docs/plans/2026-09-07-daily-use-harness-design.md` was folded into the integrated
index. Retrieve any with `git log --all -- <path>` and `git show <commit>:<path>`.
